{{ 'cinelin-tokens.css' | asset_url | stylesheet_tag }}
{{ 'cinelin-pages.css' | asset_url | stylesheet_tag }}
<script src="{{ 'cinelin-pages.js' | asset_url }}" defer="defer"></script>

<section class="cinelin-page cinelin-page--contact" data-cinelin-page>
  <div class="cinelin-page__shell">
    <nav class="cinelin-page__topline" aria-label="Page navigation">
      <button class="cinelin-page__menu-button" type="button" data-cinelin-menu-toggle aria-controls="CinelinPageMenu-{{ section.id }}" aria-expanded="false">
        <span class="visually-hidden">Open menu</span>
        {{ 'icon-hamburger.svg' | inline_asset_content }}
      </button>
      <a class="cinelin-page__topline-brand" href="{{ routes.root_url }}">X</a>
      <div class="cinelin-page__actions">
        <span class="cinelin-page__topline-meta">Contact</span>
      </div>
      <div class="cinelin-page__menu-list" id="CinelinPageMenu-{{ section.id }}" data-cinelin-menu>
        <div class="cinelin-page__menu-nav">
          <a href="{{ routes.root_url }}">Home</a>
          <a href="{{ routes.all_products_collection_url }}">Maker</a>
          <a href="{{ routes.all_products_collection_url }}">Collection</a>
          <a href="/pages/about">About</a>
          <a href="{{ routes.cart_url }}">Cart</a>
          <a href="/pages/contact">Contact</a>
        </div>
      </div>
    </nav>

    <header class="cinelin-page__hero">
      <div>
        <p class="cinelin-page__kicker">{{ section.settings.kicker | escape }}</p>
        <h1 class="cinelin-page__title">{{ section.settings.title | escape }}</h1>
      </div>
      <div>
        <p class="cinelin-page__intro">{{ section.settings.intro | escape }}</p>
        <p class="cinelin-page__copy">{{ section.settings.copy | escape }}</p>
      </div>
    </header>

    <div class="cinelin-contact">
      <div>
        <p class="cinelin-page__eyebrow">{{ section.settings.side_kicker | escape }}</p>
        <h2 class="cinelin-story__title">{{ section.settings.side_title | escape }}</h2>
        <p class="cinelin-page__copy">{{ section.settings.side_copy | escape }}</p>
      </div>

      {%- form 'contact', id: 'CinelinContactForm', class: 'cinelin-contact__form' -%}
        {%- if form.posted_successfully? -%}
          <p class="cinelin-contact__status" tabindex="-1" autofocus>{{ 'templates.contact.form.post_success' | t }}</p>
        {%- elsif form.errors -%}
          <p class="cinelin-contact__error" role="alert" tabindex="-1" autofocus>{{ 'templates.contact.form.error_heading' | t }}</p>
        {%- endif -%}

        <div class="cinelin-contact__row">
          <input
            class="cinelin-contact__field"
            autocomplete="name"
            type="text"
            id="CinelinContactForm-name"
            name="contact[{{ 'templates.contact.form.name' | t }}]"
            value="{% if form.name %}{{ form.name }}{% elsif customer %}{{ customer.name }}{% endif %}"
            placeholder="{{ 'templates.contact.form.name' | t }}"
          >
          <input
            autocomplete="email"
            type="email"
            id="CinelinContactForm-email"
            class="cinelin-contact__field"
            name="contact[email]"
            spellcheck="false"
            autocapitalize="off"
            value="{% if form.email %}{{ form.email }}{% elsif customer %}{{ customer.email }}{% endif %}"
            aria-required="true"
            {% if form.errors contains 'email' %}
              aria-invalid="true"
              aria-describedby="CinelinContactForm-email-error"
            {% endif %}
            placeholder="{{ 'templates.contact.form.email' | t }}"
          >
        </div>

        {%- if form.errors contains 'email' -%}
          <p class="cinelin-contact__error" id="CinelinContactForm-email-error">
            {{ form.errors.translated_fields.email | capitalize }}
            {{ form.errors.messages.email }}
          </p>
        {%- endif -%}

        <input
          type="tel"
          id="CinelinContactForm-phone"
          class="cinelin-contact__field"
          autocomplete="tel"
          name="contact[{{ 'templates.contact.form.phone' | t }}]"
          pattern="[0-9\-]*"
          value="{% if form.phone %}{{ form.phone }}{% elsif customer %}{{ customer.phone }}{% endif %}"
          placeholder="{{ 'templates.contact.form.phone' | t }}"
        >

        <textarea
          rows="8"
          id="CinelinContactForm-body"
          class="cinelin-contact__field"
          name="contact[{{ 'templates.contact.form.comment' | t }}]"
          placeholder="{{ 'templates.contact.form.comment' | t }}"
        >
          {{- form.body -}}
        </textarea>

        <button type="submit" class="cinelin-page__button">
          {{ 'templates.contact.form.send' | t }}
        </button>
      {%- endform -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Cinelin contact",
  "settings": [
    {
      "type": "text",
      "id": "kicker",
      "label": "Kicker",
      "default": "Contact"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Say hello"
    },
    {
      "type": "text",
      "id": "intro",
      "label": "Intro",
      "default": "Questions, custom notes, and quiet little messages."
    },
    {
      "type": "textarea",
      "id": "copy",
      "label": "Copy",
      "default": "Use this page for customer messages while the final shop operations are still being refined."
    },
    {
      "type": "text",
      "id": "side_kicker",
      "label": "Side kicker",
      "default": "Studio"
    },
    {
      "type": "text",
      "id": "side_title",
      "label": "Side title",
      "default": "We answer with care."
    },
    {
      "type": "textarea",
      "id": "side_copy",
      "label": "Side copy",
      "default": "The form keeps the same calm, high-end styling as the rest of the site, with clear fields and strong focus states."
    }
  ],
  "presets": [
    {
      "name": "Cinelin contact"
    }
  ]
}
{% endschema %}
